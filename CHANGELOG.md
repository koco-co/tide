# 变更记录

## 2.0.17 - 2026-08-21

### 修复

- 场景绑定器重新推导并逐字段绑定当前脱敏 HAR 的确定性结果，且所有 HAR 场景最多只允许第三层断言，阻断改名绕过。
- 候选校验器把清理 `finally` 收敛为唯一且可机械证明的正向 `if` 守卫，并识别布尔、假值别名、解构、海象和删除等标记变更，阻断在删除结果验证前关闭或破坏清理兜底。
- 初始化与候选校验器按 AST 名称和精确属性路径传播响应污点，覆盖解构、条件表达式、海象、链式、容器变更调用、对象根别名、`self`/`cls` 保存路径及敏感 `getattr`，阻断响应内容泄漏。
- Ruff 候选校验结果与项目身份、配置、测试目录和写入计划绑定，并在审查绑定、预检和实际写入前重新执行，阻断复用过期审查结果。

## 2.0.16 - 2026-08-14

### 修复

- 候选校验器机械阻断在删除后验证完成前清除清理标记，确保后续断言失败时仍会清理由本次测试创建的数据。
- 仅由脱敏 HAR 确定的 CRUD 场景最多绑定协议、结构和关系三层，不再无项目证据声明业务层断言。
- 初始化候选规则根据其声明的 Python 证据追踪响应内容泄漏调用链，禁止把不安全的 helper 或 fixture 固化为复用规则。

## 2.0.15 - 2026-08-14

### 修复

- 候选校验器从画像嵌套字段和项目规则证据声明统一收集证据文件，不再要求重复的顶层 `evidence` 字段。
- 两个 Skill 的临时目录都由受控脚本创建并输出，代理不得自行选择 `/tmp` 或 `/private/tmp`。
- 初始化规则机械拒绝把仓库文档或测试计划误写为“用户确认”。
- 独立审查不得因耗时被中断、催促、重启或重新委派。

## 2.0.14 - 2026-08-14

### 修复

- 将异常处理块中的隐式异常链泄漏校验下沉到确定性脚本。
- 从项目证据中追踪会引用响应正文的 fixture 与辅助方法，并阻断候选复用。
- 机械阻断固定首屏分页和未推进的分页游标，避免臆测排序与数据量。

## 2.0.13 - 2026-08-14

### 修复

- 证据归一化允许 `/api/...` 这类接口路由，同时继续阻断用户目录、Unix 常见本地根目录、Windows 盘符、UNC 与 `~` 路径进入角色产物。
- 测试候选改由当前会话按生成约束直接形成，独立角色只负责结果审查；禁止用超时中断和重复委派掩盖首轮生成失败。
- 候选安全检查能够证明固定本地失败文本和仅由安全字面量调用的辅助函数参数，同时继续阻断动态响应内容进入失败消息。
- Ruff 校验与 pytest 启动器使用一次性隔离缓存和白名单子进程环境，不再读取用户全局 `uv` 缓存或透传画像未确认的环境变量。
- `uv run --no-sync` 前机械确认目标项目已有完整 `.venv`；缺失或不完整时失败关闭，避免 Tide 隐式创建或修复项目运行环境。

## 2.0.12 - 2026-08-14

### 修复

- `init-tide` 新增角色分析归一化：脚本从描述性证据中提取并校验本轮扫描文件引用，避免模型把证据数组写成说明文字时直接中断。
- 测试运行器不再由角色复制，绑定脚本直接采用确定性扫描结果，减少机械字段漂移。
- 初始化工作流只让角色负责语义分析，字段归一化、证据去重与运行器绑定均由脚本完成。

## 2.0.11 - 2026-08-14

### 修复

- 生成角色只返回场景编号与候选文件；目标地址变量、必需运行时变量和生成状态全部由脚本从当前画像与场景覆盖确定，不再让角色重复填写机械字段。
- 新增独立候选校验脚本，机械拒绝可能泄漏响应内容的原生断言、直接抛异常、吞异常、运行时对象输出、不安全失败消息、越界重定向与无项目证据的字面量查询参数。
- Tide 结果审查与初始化规则审查都只返回语义结论；计划、文件范围、画像和规则摘要统一由脚本在审查后绑定，避免角色复制摘要产生漂移。
- 候选代码校验从事务写入脚本拆分为独立职责，并在 Python 3.8 自检与 Skill 严格校验中单独覆盖。

## 2.0.10 - 2026-08-14

### 修复

- `init-tide` 由扫描脚本根据根目录锁文件与 pytest 配置共同确定安全 runner，角色只能复制脚本结果，绑定阶段拒绝任何漂移。
- `tide` 由本地脚本从脱敏 HAR 摘要确定性识别完整 CRUD 时序并排除失败响应、静态资源和重复记录，删除原场景分析角色，缩短首次确认前等待时间。

## 2.0.9 - 2026-08-14

### 修复

- 删除会让 Codex 隐藏整个插件的 `disable-model-invocation` 字段；Codex 继续通过 `agents/openai.yaml` 的 `allow_implicit_invocation: false` 限制为显式调用。
- 该字段删除后 Claude Code 侧不再有模型侧显式调用闸门；Skill 的三道确认门与脚本摘要门禁仍拦截任何未经确认的写入或执行。
- 真实安装验收新增“插件必须出现在 Codex 显式调用菜单中”的发现性门禁，避免把安装列表中的启用状态误报为 Skill 可用。

## 2.0.8 - 2026-08-14

### 修复

- 脱敏摘要、场景分析、场景确认、候选场景覆盖、目标 Python 兼容性、runner 白名单和临时目录清理由确定性脚本校验，不再依赖角色按提示词自觉执行机械规则。
- 主机、路径段和敏感字段别名改为按首次出现顺序分配的不可逆编号，不再暴露可被字典猜测的无盐散列别名；相同 HAR 仍生成稳定摘要。
- 初始化读取范围绑定目录身份和用户确认摘要；画像、角色结论与动态规则中的证据引用逐项核对本轮扫描文件清单。
- 初始化扫描在角色委派前机械排除 `.env`、HAR、私钥、证书和常见凭据文件，角色结果不能引用扫描清单外路径。
- 已确认 pytest runner 必须同时具备安全命令、对应锁文件和独立 pytest 配置证据；启动器统一从画像读取 runner，直接调用执行器会失败关闭。
- 真实执行只接受字面 IP 目标，并要求画像证明确认目标来自环境变量且自动重定向关闭；域名、DNS 漂移风险或缺少 transport 证据时只生成不执行。
- Skill 说明、角色提示词与工作流统一使用中文，公共内容不包含宿主命令语法、`model` 或 `tools` 字段；仅保留校验规范强制的六个结构章节键。

## 2.0.7 - 2026-08-14

### 变更

- 生成角色只负责场景与测试代码候选，不再生成运行编号、环境资格或项目证据摘要。
- `write_run.py` 统一生成运行编号、计算并绑定摘要、核对画像环境变量、校验环境枚举与 Python 语法，并在证据齐全时调用项目锁定 Ruff。
- `bind_init_plan.py` 逐项验证环境变量确实出现在所列项目文件中，并机械核对 pytest runner 的锁文件、配置和命令证据。
- 角色提示词删除可以由脚本完成的格式、摘要、路径、范围、环境枚举和静态检查判断，只保留场景、断言与执行边界等语义职责。

## 2.0.6 - 2026-08-14

### 修复

- 目标项目已在配置与锁文件中声明静态检查器时，Tide 必须用既有锁定工具链对系统临时目录中的候选执行无缓存、无修复检查；检查失败必须修正并复审，不再把可运行但违反项目规范的代码写入测试目录。

## 2.0.5 - 2026-08-14

### 修复

- 场景确认与执行确认统一使用执行器的精确安全环境枚举，不再把 `local-docker-compose` 等描述性别名当作可执行环境，避免测试写入后才被安全门禁阻断。

## 2.0.4 - 2026-08-14

### 修复

- `init-tide` 明确要求额外源码逻辑标签使用小写短横线格式，避免先生成无效标签再重试。
- `tide` 在生成前绑定符合校验器要求的小写运行编号，不再因时间戳大写字符触发机械预检失败。
- “不重复现有测试”只禁止新增语义重复的独立用例；复合场景可以使用已有证据的接口作为必要中间步骤，不再错误替换为无证据接口。
- 执行计划只绑定必需环境变量名及存在性，不再对凭据值生成可持久化散列，避免形成低熵凭据的离线猜测校验材料。
- 两个 Skill 保留 Claude Code 的显式调用安全字段，并由 `agents/openai.yaml` 分别约束 Codex；公共工作流与内容仍只维护一份。

## 2.0.3 - 2026-08-14

### 修复

- `init-tide` 根据项目锁文件、配置或已有文档绑定 pytest runner；证据不足时保持未知，不再默认使用宿主 Python。
- `tide` 使用画像已确认的 runner 启动执行脚本；对 uv 项目使用 `uv run --locked --no-sync python`，不安装或同步依赖。
- 执行预检会先验证当前解释器能导入 pytest；运行时未就绪时在零测试、零请求状态下标记阻断，不再误报为测试失败。

## 2.0.2 - 2026-08-14

### 修复

- `tide` 在用户未明确提供 HAR 路径时只请求路径，不再搜索或自动选择项目内 HAR。
- 额外源码的逻辑标签不再被当作文件系统相对路径；当前交互没有已授权映射时，只消费画像与规则中的已持久化事实。
- 两个 Skill 都使用 Python `tempfile` 返回的系统临时根目录，避免在 macOS 上先因硬编码 `/tmp` 路径失败。

## 2.0.1 - 2026-08-14

### 修复

- `init-tide` 在项目画像中记录既有测试真实读取的必需环境变量名与证据，不读取或持久化变量值。
- `tide` 的写入计划与执行摘要绑定已证实的必需环境变量名，pytest 子进程仅透传该白名单的当前值。
- 缺失必需变量、出现宿主保留变量或未经画像证据确认的名称时，真实执行失败关闭。

## 2.0.0 - 2026-08-13

### 变更

- 将 Tide 重构为纯 Skills 插件，`skills/` 成为唯一工作流来源。
- 将项目初始化入口从 `using-tide` 更名为 `init-tide`，不提供别名、跳转或兼容层。
- 所有 Skill、角色提示词、规则、工作流与插件说明统一使用中文。
- 项目代码风格、测试结构和目录规范改为初始化时根据目标项目动态生成。
- 原始 HAR 改由 Skill 内的标准库脚本在本地脱敏，模型仅接收安全摘要。
- 写入范围限制为 `.tide/` 与用户确认的测试目录，已有文件冲突时失败关闭。

### 删除

- 删除 Python 核心、重复宿主包、旧命令适配、固定项目模板、固定技术栈规则和行业断言。
- 删除仓库同步、外部通知、新项目脚手架、后台任务、归档恢复和非交互确认。
- 删除英文 README、历史设计文档、迁移脚本与其他退役产物。
