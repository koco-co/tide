## installed hook smoke
$ printf ... | python3 installed claude_hooks.py user-prompt-submit
{"hookSpecificOutput": {"hookEventName": "UserPromptSubmit", "additionalContext": "Tide hook detected a HAR-to-interface-test generation request. Use the Claude Code SlashCommand tool to invoke `/tide:tide` for this request; in plugin CLI/non-interactive contexts the reliable command form is `/tide:tide <har-file-or-directory> --yes --non-interactive`. 不要自由生成测试文件；必须执行 Tide workflow，先创建 `.tide/run-context.json` 和 `.tide/write-scope-snapshot.json`，再解析 HAR、生成场景、写测试、运行校验。Target writes are limited to `.tide/` and `testcases/`; do not modify `api/`, `dao/`, `utils/`, `config/`, `testdata/`, or `resource/`."}, "suppressOutput": true}

$ printf ... | python3 installed claude_hooks.py pre-tool-use
{"decision": "block", "reason": "Tide write-scope violation: utils/x.py is outside .tide/ and testcases/"}

## namespaced command
## /tide:tide — HAR → pytest 测试套件生成工具

**用途：** 从 HAR 文件解析 API 请求，结合源码分析，生成带 L1-L5 分层断言的 pytest 测试套件。

### 用法

```
/tide:tide <har_file> [选项]
```

### 参数

| 参数 | 说明 |
|------|------|
| `har_file` | `.har` 文件路径（必需） |
| `--quick` / `--yes` / `--non-interactive` | 使用保守默认值跳过确认 |
| `--resume` | 从已有 `.tide` 状态继续 |
| `--wave N` | 从指定 Tide wave 继续 |

### 工作流程

1. **解析 HAR** — 通过 Tide 确定性脚本验证并解析 HAR 文件
2. **场景分析** — 分析源码，为测试场景补充 CRUD 闭环和边界条件
3. **生成测试** — 生成带 L1-L5 分层断言的 pytest 代码
4. **审查 & 执行** — 审查生成的代码，运行最小有效验证范围
5. **汇总** — 输出生成文件、检查项、失败分类及人工跟进项

### 底层技能栈

工作流涉及多个子技能（均为 Tide 内置 agent）：

- `har-parser` — 解析 HAR，提取结构化端点数据
- `project-scanner` — 扫描项目架构、代码风格、鉴权方式
- `scenario-analyzer` — 分析 CRUD 闭环和边界条件
- `case-writer` — 生成 L1-L5 分层断言 pytest 代码
- `case-reviewer` — 审查并修复生成的测试代码

### 示例

```bash
